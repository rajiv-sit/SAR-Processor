# Dataset Notes

This repo includes local sample datasets under the repository root:
- `RPFAccum1_files/` and `FLT104_H_circle_leg4_C1_rpf/` for RPF integration tests.

These datasets are used by integration tests that parse `.rpf/.wnf` files.
If you move the dataset folders, update the tests or set a symlink.
